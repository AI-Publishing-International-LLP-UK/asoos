/**
 * Dr. Claude Project Management Commands
 * 
 * This module registers project-related commands for Dr. Claude's
 * project management capabilities in the Aixtiv CLI.
 */

const list = require('./list');

module.exports = {
  list
};

